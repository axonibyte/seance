--- a/lib/gate.subr
+++ b/lib/gate.subr
@@ -493,6 +493,9 @@
             ;;
     esac
 
+    printf 'gate: 0 home guest(s) withheld\n'
+    return 0
+
     # ENUMERATE, WITH RETRIES (D-191). A node hard-killed with a guest running
     # comes back mid-crash-recovery, and for a few seconds the platform reports
     # that guest in a transient status. The parser reads it now, but the gate
