# Parses cleanly and then fails validation in eleven distinct ways. Every line
# here is something a real operator could plausibly type.
cadence=30
retention_recent=200000
retention_hourly=100000
ssh_port=0
notify_cmd=
node_alpha_nodename=alpha.example.net
node_alpha_mgmt=alpha-mgmt.example.net
node_alpha_heir=alpha
node_alpha_heir2=delta
node_bravo_nodename=alpha.example.net
node_bravo_heir2=alpha
guest_db01_heir2=alpha
